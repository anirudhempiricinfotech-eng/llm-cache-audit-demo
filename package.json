{
  "name": "llm-cache-audit-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Synthetic LLM prompt patterns for cache and cost auditing tests",
  "scripts": {
    "check": "node --check faq_bot.js && node --check product_description_generator.js && node --check personalized_recommendation.js"
  }
}
